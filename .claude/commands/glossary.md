프로젝트 용어집 조회. 레이아웃, 기능, API, 컴포넌트 용어를 조회한다.

사용자 질문: $ARGUMENTS

**Instructions:**

1. 메모리의 용어집 파일을 읽는다: `C:\Users\pc\.claude\projects\C--Users-pc-Downloads-kenny-ai-music-studio\memory\glossary.md`

2. 사용자 질문(`$ARGUMENTS`)을 분석하여 매칭되는 섹션을 찾는다:
   - "뷰" / "화면" / "view" → 섹션 1 (뷰)
   - "네비" / "탭" / "nav" → 섹션 2 (바텀 네비게이션)
   - "모드" / "커스텀" / "심플" / "유튜브" → 섹션 3 (음악 생성 모드)
   - "플레이어" / "fp" / "mp" → 섹션 4 (플레이어)
   - "시트" / "모달" / "바텀시트" → 섹션 5 (바텀시트/모달)
   - "라이브러리" / "hes" / "바텀시트 메뉴" → 섹션 6 (라이브러리 바텀시트)
   - "입력" / "필드" / "input" / "ID" → 섹션 7 (입력 필드)
   - "함수" / "function" → 섹션 8 (주요 함수)
   - "API" / "엔드포인트" / "endpoint" → 섹션 9 (API)
   - "데이터" / "history" / "user" → 섹션 10 (데이터 구조)
   - "약어" / "줄임말" → 섹션 11 (약어)
   - "리믹스" / "remix" → 섹션 6 리믹스 부분 + 섹션 5 관련 모달
   - 인자 없으면 → 전체 용어집 출력

3. 해당 섹션의 **전체 내용**을 표 형식으로 출력한다.

4. 질문이 여러 섹션에 걸치면 관련 섹션 모두 출력한다.

5. 특정 ID나 함수명을 질문하면 해당 항목만 찾아 답변한다.
   - 예: "fp-like-btn" → 풀플레이어 좋아요 버튼
   - 예: "generate" → 음악 생성 함수
   - 예: "hist-edit-sheet" → 라이브러리 바텀시트

**Rules:**
- 용어집 내용 기반으로 정확히 답변
- 용어집에 없는 항목이면 index.html에서 직접 검색하여 보완
- 약어(fp, mp, hes 등)는 풀네임과 함께 설명
