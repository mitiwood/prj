# 시퀀스 다이어그램

## 1. 음악 생성 플로우

```
사용자          프론트엔드(index.html)     /api/check-credit    /api/kie(proxy)     kie.ai API         Supabase        텔레그램/카카오
  │                    │                       │                    │                  │                 │                │
  │──[생성 클릭]──────→│                       │                    │                  │                 │                │
  │                    │──checkPlanLimit()─────→│                    │                  │                 │                │
  │                    │                       │──plan/usage 조회──→│                  │                 │                │
  │                    │                       │←──OK/limit_exceeded│                  │                 │                │
  │                    │←──허용/차단────────────│                    │                  │                 │                │
  │                    │                       │                    │                  │                 │                │
  │                    │──buildOptimalPrompt()  │                    │                  │                 │                │
  │                    │──kieRequest(POST)─────────────────────────→│                  │                 │                │
  │                    │                       │                    │──POST /generate──→│                 │                │
  │                    │                       │                    │←──{taskId}────────│                 │                │
  │                    │←──{taskId}────────────────────────────────│                  │                 │                │
  │                    │                       │                    │                  │                 │                │
  │                    │──pollResult(500ms~3s)─────────────────────→│                  │                 │                │
  │                    │                       │                    │──GET record-info─→│                 │                │
  │                    │                       │                    │←──{status,tracks}─│                 │                │
  │                    │←──{tracks}────────────────────────────────│                  │                 │                │
  │                    │                       │                    │                  │                 │                │
  │                    │──renderTracks(A/B)     │                    │                  │                 │                │
  │                    │──trackUsage('song')    │                    │                  │                 │                │
  │                    │──_serverCreditDeduct()─→│                    │                  │                 │                │
  │                    │──_saveTrackToServer()──────────────────────────────────────────────────────────→│                │
  │                    │                       │                    │                  │                 │──알림 발송────→│
  │←──[결과 표시]──────│                       │                    │                  │                 │                │
```

## 2. 소셜 로그인 플로우

```
사용자          프론트엔드         /api/auth/{provider}    OAuth 프로바이더     /api/auth/callback     Supabase
  │                │                      │                      │                    │                 │
  │──[로그인 클릭]─→│                      │                      │                    │                 │
  │                │──redirect────────────→│                      │                    │                 │
  │                │                      │──OAuth 요청──────────→│                    │                 │
  │                │                      │                      │                    │                 │
  │←──[OAuth 동의화면]────────────────────────────────────────────│                    │                 │
  │──[동의]────────────────────────────────────────────────────→│                    │                 │
  │                │                      │                      │──code 반환────────→│                 │
  │                │                      │                      │                    │──token 교환─────→│
  │                │                      │                      │                    │──user upsert────→│
  │                │                      │                      │                    │←─{user}──────────│
  │←──[redirect + user 정보]──────────────────────────────────────────────────────────│                 │
  │                │──localStorage 저장    │                      │                    │                 │
```

## 3. 결제 플로우

```
사용자          프론트엔드         Toss 위젯          /api/payments/success     Supabase        텔레그램
  │                │                  │                       │                    │              │
  │──[업그레이드]──→│                  │                       │                    │              │
  │                │──upgradePlan()   │                       │                    │              │
  │                │──_initToss()     │                       │                    │              │
  │                │──requestPayment()→│                       │                    │              │
  │←──[카드 입력 UI]──────────────────│                       │                    │              │
  │──[카드 정보]───────────────────→│                       │                    │              │
  │                │                  │──successUrl redirect──→│                    │              │
  │                │                  │                       │──Toss API 확인────→│              │
  │                │                  │                       │──payments INSERT───→│              │
  │                │                  │                       │──users.plan UPDATE──→│              │
  │                │                  │                       │──텔레그램 알림──────────────────→│
  │←──[redirect /?payment=success]────────────────────────────│                    │              │
  │                │──localStorage plan 갱신                   │                    │              │
  │                │──renderPlanUI()  │                       │                    │              │
```

## 4. 실시간 알림 플로우

```
관리자(봇/admin)     /api/live-notify     Supabase      사용자(폴링)         프론트엔드
      │                    │                 │               │                   │
      │──POST {title,body}─→│                 │               │                   │
      │                    │──INSERT──────────→│               │                   │
      │                    │──텔레그램+카카오 알림               │                   │
      │←──{ok}─────────────│                 │               │                   │
      │                    │                 │               │                   │
      │                    │                 │    (10초 폴링)  │                   │
      │                    │                 │←──GET ?since=ts─│                   │
      │                    │                 │──{notifications}→│                   │
      │                    │                 │               │──토스트 팝업 표시──→│
```

## 5. 텔레그램 봇 명령 플로우

```
관리자(텔레그램)      Telegram API       /api/tg-webhook      Supabase/GitHub      텔레그램 응답
      │                   │                    │                    │                   │
      │──"수정 버튼 색상"──→│                    │                    │                   │
      │                   │──POST webhook──────→│                    │                   │
      │                   │                    │──파싱: cmd=수정     │                   │
      │                   │                    │──GitHub Issue 생성──→│                   │
      │                   │                    │←──{issue #N}────────│                   │
      │                   │                    │──tgSend("Issue 등록")────────────────→│
      │                   │                    │                    │                   │
      │                   │                    │          [GitHub Actions 트리거]        │
      │                   │                    │                    │──Claude Code 실행  │
      │                   │                    │                    │──PR 생성           │
      │                   │                    │                    │──텔레그램 알림─────→│
      │                   │                    │                    │                   │
      │──"머지"────────────→│──POST webhook──────→│                    │                   │
      │                   │                    │──GitHub API merge──→│                   │
      │                   │                    │──tgSend("머지완료")──────────────────→│
      │                   │                    │                    │──Vercel 자동배포   │
```

## 6. 커뮤니티 재생 동기화 플로우

```
사용자              커뮤니티 리스트          미니플레이어           풀플레이어
  │                      │                      │                    │
  │──[▶ 클릭]────────────→│                      │                    │
  │                      │──commPlaySb(sbId)     │                    │
  │                      │──같은 곡? → 토글      │                    │
  │                      │──다른 곡? → stopAll   │                    │
  │                      │──showMiniPlayer()─────→│                    │
  │                      │──버튼 ⏸ 변경          │                    │
  │                      │                      │──audio.play()       │
  │                      │                      │──timeupdate────────→│ (열려있으면)
  │                      │                      │                    │──프로그레스 동기화
  │                      │                      │                    │
  │──[미니 ⏸ 클릭]───────────────────────────────→│                    │
  │                      │                      │──audio.pause()      │
  │                      │←──_syncCommPlayBtn(▶)──│                    │
  │                      │                      │                    │
  │──[곡 종료]──────────────────────────────────→│                    │
  │                      │←──_resetAllCommPlayBtns│                    │
  │                      │                      │──다음 곡 자동 재생   │
```
